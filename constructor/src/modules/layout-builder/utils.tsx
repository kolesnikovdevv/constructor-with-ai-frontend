export const getComponent = (type, id, setFormObjects, formObjects) => {
  if (type === "text") return <button className=" "></button>
  else if (type === "image") return <button className=""></button>
}
