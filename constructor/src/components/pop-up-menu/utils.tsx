export const getComponent = (type, id, setFormObjects, formObjects) => {
  if (type === "button") return <button className="">button</button>
  else if (type === "input") return <button>input</button>
}
